# 日常工作流程

## ✅ 第一次推送已完成

你的博客代码已成功推送到 GitHub！

**仓库地址：** https://github.com/x1ngg3/x1ngg3.github.io

**下一步：**
1. 访问 https://github.com/x1ngg3/x1ngg3.github.io
2. 进入 `Settings` → `Pages`
3. Source 选择 `GitHub Actions`
4. 等待几分钟，你的博客就会发布到 `https://x1ngg3.github.io/`

---

## 📝 日常写作工作流程

### 方式 1: 添加新文章

```bash
# 1. 在 src/content/posts/ 创建新文章
# 例如：src/content/posts/2024-01-25-my-new-post.md

# 2. 提交并推送
git add .
git commit -m "新文章：文章标题"
git push
```

**注意：** 第一次推送后，以后只需要 `git push`，不需要 `-u origin main` 了！

### 方式 2: 添加说说

```bash
# 1. 在 src/content/thoughts/ 创建新说说
# 例如：src/content/thoughts/2024-01-25-thought.md

# 2. 提交并推送
git add .
git commit -m "新说说"
git push
```

### 方式 3: 修改样式/配置

```bash
# 1. 修改文件（如 src/styles/global.css）

# 2. 提交并推送
git add .
git commit -m "更新主题颜色"
git push
```

---

## 🎨 常见操作示例

### 写新文章

```bash
# 1. 创建文章
cat > src/content/posts/2024-01-25-hello-world.md << 'EOF'
---
title: "Hello World"
description: "我的第一篇文章"
date: 2024-01-25
tags: ["随笔"]
---

## 这是我的第一篇文章

内容...
EOF

# 2. 本地预览（可选）
npm run dev
# 访问 http://localhost:4321/posts/hello-world 查看效果

# 3. 提交推送
git add .
git commit -m "新文章：Hello World"
git push
```

### 修改主题色

```bash
# 1. 编辑 src/styles/global.css
# 找到 --color-primary: #3b82f6;
# 改成你喜欢的颜色，如 --color-primary: #8b5cf6;

# 2. 提交推送
git add src/styles/global.css
git commit -m "更改主题色为紫色"
git push
```

### 添加图片

```bash
# 1. 复制图片到 public/images/
cp ~/Downloads/my-photo.jpg public/images/

# 2. 在文章中引用
# ![我的照片](/images/my-photo.jpg)

# 3. 提交推送
git add public/images/my-photo.jpg
git commit -m "添加图片"
git push
```

### 添加评论

```bash
# 1. 编辑 public/comments/comments.json
# 添加新评论（参考 COMMENTS_GUIDE.md）

# 2. 提交推送
git add public/comments/comments.json
git commit -m "添加新评论"
git push
```

---

## ⚡ 快捷命令

### 三合一命令

```bash
# 快速提交并推送
git add . && git commit -m "更新" && git push
```

### 查看状态

```bash
# 查看修改了哪些文件
git status

# 查看具体改动内容
git diff

# 查看提交历史
git log --oneline -10
```

### 撤销操作

```bash
# 撤销工作区的修改（文件还没 add）
git checkout -- 文件名

# 撤销暂存区的修改（已经 add 但没 commit）
git reset HEAD 文件名

# 撤销最后一次 commit（但保留修改）
git reset --soft HEAD^

# 修改最后一次 commit 信息
git commit --amend -m "新的提交信息"
```

---

## 🔄 自动部署流程

每次 `git push` 后会自动触发：

1. **GitHub Actions 启动**
   - 检查代码
   - 安装依赖
   - 构建网站

2. **部署到 GitHub Pages**
   - 生成静态文件
   - 发布到 `https://x1ngg3.github.io/`

3. **完成**
   - 等待 1-2 分钟
   - 刷新网站查看更新

**查看部署状态：**
- 访问 https://github.com/x1ngg3/x1ngg3.github.io/actions
- 查看最新的 workflow 运行状态
- 绿色 ✅ = 部署成功
- 红色 ❌ = 部署失败（查看日志）

---

## 📋 完整工作流程总结

```
写文章/修改代码
    ↓
本地预览（可选）
npm run dev
    ↓
提交到 Git
git add .
git commit -m "描述"
    ↓
推送到 GitHub
git push
    ↓
自动部署
（GitHub Actions）
    ↓
更新完成
刷新网站查看
```

---

## ⚠️ 重要提示

### 第一次推送 vs 后续推送

```bash
# ✅ 第一次推送（已完成）
git push -u origin main

# ✅ 以后每次推送
git push
```

**`-u origin main` 只需要第一次用！** 它的作用是设置上游分支，以后就不需要了。

### Commit 信息建议

好的 commit 信息：
```bash
git commit -m "新文章：如何使用 Astro 构建博客"
git commit -m "修复文章卡片点击问题"
git commit -m "更新关于页面的个人信息"
git commit -m "添加新的示例图片"
```

不好的 commit 信息：
```bash
git commit -m "更新"  # 太简略
git commit -m "fix"   # 看不出修复了什么
git commit -m "aaa"   # 无意义
```

### 推送前检查

```bash
# 1. 查看将要提交什么
git status

# 2. 查看具体改动
git diff

# 3. 确认无误后再 push
git push
```

---

## 🆘 遇到问题？

### 推送失败

```bash
# 先拉取最新代码
git pull

# 解决冲突后再推送
git push
```

### 忘记提交信息

```bash
# 如果 commit 后发现信息写错了
git commit --amend -m "正确的提交信息"
git push --force  # 如果已经 push 了，需要强制推送
```

### 构建失败

1. 查看 GitHub Actions 日志
2. 本地运行 `npm run build` 测试
3. 修复错误后重新推送

---

## 💡 小贴士

1. **经常提交**：每完成一个小改动就提交，不要积攒
2. **有意义的 commit**：让以后的自己知道改了什么
3. **推送前测试**：`npm run dev` 本地预览确认无误
4. **查看 Actions**：确保每次部署都成功
5. **备份重要内容**：虽然 Git 就是备份，但重要文章可以额外保存

---

## 🎯 你现在可以做什么

1. **查看 GitHub 仓库**
   ```
   https://github.com/x1ngg3/x1ngg3.github.io
   ```

2. **配置 GitHub Pages**
   - Settings → Pages → Source: GitHub Actions

3. **等待部署**
   - Actions 标签页查看进度
   - 等待绿色 ✅

4. **访问网站**
   ```
   https://x1ngg3.github.io/
   ```

5. **开始写第一篇文章**
   - 在 `src/content/posts/` 创建 `.md` 文件
   - 参考 `welcome.md` 的格式

---

**恭喜！你已经掌握完整的博客工作流程了！** 🎉

以后只需要：
```bash
git add .
git commit -m "xxx"
git push
```

就这么简单！
