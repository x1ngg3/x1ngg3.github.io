# GitHub Pages 部署完整指南

本指南详细说明如何将博客部署到 GitHub Pages，包含**截图说明**和**常见问题**。

## 📋 部署前检查清单

- [ ] 已安装 Git
- [ ] 有 GitHub 账号
- [ ] 博客在本地运行正常（`npm run dev` 没有错误）
- [ ] 已修改个人信息（网站标题、关于页面等）

## 🚀 部署步骤

### 步骤 1: 在 GitHub 创建仓库

1. 访问 https://github.com
2. 点击右上角的 `+` → `New repository`
3. 填写信息：
   - **Repository name**: `blog` （或任何你喜欢的名字）
   - **Description**: "我的个人博客" （可选）
   - **Public/Private**: 选择 `Public` （Public 才能使用免费的 GitHub Pages）
   - ❗ **不要勾选** "Add a README file"（我们已经有了）
4. 点击 `Create repository`

### 步骤 2: 初始化本地 Git 仓库

在你的博客项目目录下打开终端：

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 创建第一个提交
git commit -m "Initial commit: 博客初始化"
```

### 步骤 3: 关联远程仓库并推送

**重要：** 把下面的 `x1ngg3` 替换成你的 GitHub 用户名！

```bash
# 关联远程仓库
git remote add origin https://github.com/x1ngg3/blog.git

# 检查是否关联成功
git remote -v

# 设置主分支名称
git branch -M main

# 推送到 GitHub
git push -u origin main
```

第一次推送时可能需要登录 GitHub：
- **Username**: 你的 GitHub 用户名
- **Password**: 使用 Personal Access Token（不是密码！）

### 步骤 4: 生成 GitHub Personal Access Token（首次推送需要）

如果推送时提示需要密码：

1. 访问 https://github.com/settings/tokens
2. 点击 `Generate new token` → `Generate new token (classic)`
3. 设置：
   - **Note**: "Blog deployment"
   - **Expiration**: 选择 `No expiration`（永不过期）
   - **Select scopes**: 勾选 `repo` （所有子选项会自动勾选）
4. 点击 `Generate token`
5. **复制 token**（只显示一次，记得保存！）
6. 在 git push 时：
   - Username: 你的 GitHub 用户名
   - Password: 粘贴刚才的 token

### 步骤 5: 配置 GitHub Pages

1. 打开你的 GitHub 仓库页面
2. 点击 `Settings`（设置）
3. 在左侧菜单找到 `Pages`
4. 在 **Source** 部分：
   - **Source**: 选择 `GitHub Actions` （不是 Deploy from a branch！）
5. 页面会显示：
   ```
   Your site is ready to be published at https://x1ngg3.github.io/blog/
   ```

### 步骤 6: 等待部署完成

1. 回到仓库首页
2. 点击顶部的 `Actions` 标签
3. 你会看到一个正在运行的工作流：`Deploy to GitHub Pages`
4. 等待几分钟，直到显示绿色的 ✅
5. 访问你的网站：`https://x1ngg3.github.io/blog/`

**注意：** 如果仓库名是 `x1ngg3.github.io`（和用户名相同），网址是 `https://x1ngg3.github.io/`（没有 `/blog`）

## ⚙️ 配置自定义域名（可选）

如果你有自己的域名（如 `blog.example.com`）：

### 在 GitHub 设置

1. Settings → Pages → Custom domain
2. 输入你的域名：`blog.example.com`
3. 点击 Save

### 在域名提供商设置

添加 DNS 记录：

**方式 1: CNAME（推荐用于子域名）**
```
类型: CNAME
名称: blog
值: x1ngg3.github.io
```

**方式 2: A 记录（用于顶级域名）**
```
类型: A
名称: @
值: 185.199.108.153
值: 185.199.109.153
值: 185.199.110.153
值: 185.199.111.153
```

### 配置项目

在 `public/` 目录创建 `CNAME` 文件：

```bash
echo "blog.example.com" > public/CNAME
```

然后推送：
```bash
git add public/CNAME
git commit -m "Add custom domain"
git push
```

## 📝 日常更新流程

写完新文章后：

```bash
# 查看更改
git status

# 添加文件
git add .

# 提交
git commit -m "新文章：文章标题"

# 推送
git push
```

等待 1-2 分钟，GitHub Actions 会自动构建和部署！

## 🔍 查看部署状态

### 方法 1: 通过 Actions 页面

1. 进入仓库
2. 点击 `Actions` 标签
3. 查看最新的工作流运行状态

### 方法 2: 通过提交记录

在仓库首页，每个 commit 旁边会有状态图标：
- ✅ 绿色对勾：部署成功
- ❌ 红色叉号：部署失败
- 🟡 黄色圆圈：正在部署

点击图标可以查看详细日志。

## 🐛 常见问题

### 问题 1: 推送时提示 `Permission denied`

**原因：** 没有权限或 token 过期

**解决：**
```bash
# 重新生成 Personal Access Token
# 然后清除缓存的凭证
git credential reject https://github.com

# 再次推送时会要求重新输入
git push
```

### 问题 2: GitHub Actions 构建失败

**错误信息：** `npm ci` 失败或构建错误

**解决：**
1. 点击 Actions → 失败的工作流 → 查看详细日志
2. 常见原因：
   - package-lock.json 不存在：本地运行 `npm install` 生成
   - 依赖版本冲突：更新依赖 `npm update`
   - 文章格式错误：检查 Front Matter 格式

### 问题 3: 网站显示 404

**原因 1：** 路径配置错误

检查 `astro.config.mjs`：
```javascript
// 如果仓库名是 blog，需要设置 base
export default defineConfig({
  site: 'https://x1ngg3.github.io',
  base: '/blog',  // 👈 取消注释这行
})
```

**原因 2：** Pages 没有启用

检查 Settings → Pages 是否选择了 `GitHub Actions`

### 问题 4: 样式丢失或页面空白

**原因：** base 路径配置错误

**解决：**
- 如果仓库名是 `x1ngg3.github.io`：删除或注释掉 `base`
- 如果仓库名是其他（如 `blog`）：设置 `base: '/blog'`

### 问题 5: 图片不显示

**原因：** 图片路径错误

**正确用法：**
```markdown
![图片](/images/pic.jpg)  ✅ 正确

![图片](images/pic.jpg)   ❌ 错误（缺少开头的 /）
![图片](./images/pic.jpg) ❌ 错误（相对路径）
```

### 问题 6: 更新没有生效

**可能原因：**
1. 浏览器缓存：按 `Ctrl + Shift + R` 强制刷新
2. GitHub Pages 缓存：等待 5-10 分钟
3. 构建失败：检查 Actions 页面

### 问题 7: 首次部署后访问 404

等待 5-10 分钟，GitHub Pages 需要时间生效。

## 📊 部署状态徽章（可选）

在 README.md 添加部署状态徽章：

```markdown
![Deploy Status](https://github.com/x1ngg3/blog/actions/workflows/deploy.yml/badge.svg)
```

## 🔒 私有仓库部署

免费的 GitHub Pages 只支持 Public 仓库。

如果需要私有仓库 + GitHub Pages：
- 需要 GitHub Pro 账号（学生可免费申请）
- 或考虑其他平台（Vercel、Netlify）

## 🚀 其他部署平台

如果不想用 GitHub Pages，还可以部署到：

### Vercel（推荐）
1. 访问 https://vercel.com
2. 导入 GitHub 仓库
3. Framework 选择 `Astro`
4. 点击 Deploy
5. 自动部署完成！

### Netlify
1. 访问 https://netlify.com
2. Add new site → Import from Git
3. 选择仓库
4. Build command: `npm run build`
5. Publish directory: `dist`
6. Deploy

### Cloudflare Pages
1. 访问 https://pages.cloudflare.com
2. Create a project
3. 连接 GitHub
4. Framework preset: `Astro`
5. Deploy

## 💡 小贴士

1. **定期推送**：养成写完就推送的习惯
2. **有意义的 commit**：方便以后查找历史
3. **检查 Actions**：确保每次都部署成功
4. **备份 Token**：把 Personal Access Token 保存到密码管理器
5. **使用 .gitignore**：不要提交 `node_modules/` 和 `dist/`（已配置好）

## 🎯 部署完成检查清单

- [ ] 代码成功推送到 GitHub
- [ ] GitHub Actions 构建成功（绿色 ✅）
- [ ] 能访问网站 URL
- [ ] 首页正常显示
- [ ] 文章列表页正常
- [ ] 文章详情页正常
- [ ] 图片正常加载
- [ ] 样式正常

---

**恭喜！你的博客已经成功部署到互联网上了！** 🎉

有任何问题，检查 GitHub Actions 日志或让 AI 帮忙解决！
