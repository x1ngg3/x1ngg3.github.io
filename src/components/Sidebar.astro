---
import { getCollection } from 'astro:content';

const allPosts = (await getCollection('posts'))
  .filter(post => !post.data.draft)
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());

const currentPath = Astro.url.pathname;
---

<aside class="sidebar">
  <nav class="sidebar-nav">
    <div class="sidebar-header">
      <h3 class="sidebar-title">所有文章</h3>
      <span class="post-count">{allPosts.length} 篇</span>
    </div>
    <ul class="post-list">
      {allPosts.map((post) => {
        const isActive = currentPath.includes(post.slug);
        return (
          <li>
            <a
              href={`/posts/${post.slug}`}
              class={`post-link ${isActive ? 'active' : ''}`}
            >
              <span class="post-link-title">{post.data.title}</span>
              <time class="post-link-date">
                {post.data.date.toLocaleDateString('zh-CN', {
                  year: 'numeric',
                  month: '2-digit',
                  day: '2-digit'
                })}
              </time>
            </a>
          </li>
        );
      })}
    </ul>
  </nav>
</aside>

<style>
  .sidebar {
    position: fixed;
    left: 0;
    top: 5.5rem;
    width: 280px;
    height: calc(100vh - 5.5rem);
    overflow-y: auto;
    background: #fdfdf8;
    padding: 2rem 0;
  }

  .sidebar-nav {
    padding: 0 2rem 0 max(2rem, calc((100vw - 1600px) / 2 + 2rem));
  }

  .sidebar-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #f3f4f6;
  }

  .sidebar-title {
    margin: 0;
    font-size: 0.875rem;
    font-weight: 600;
    color: #111827;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .post-count {
    font-size: 0.75rem;
    color: #9ca3af;
    font-weight: 500;
  }

  .post-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .post-list li {
    margin: 0;
  }

  .post-link {
    display: block;
    padding: 0.75rem 1rem;
    margin: 0 -1rem;
    border-radius: 6px;
    text-decoration: none;
    transition: all 0.2s;
  }

  .post-link:hover {
    background: #f9fafb;
  }

  .post-link.active {
    background: #f3f4f6;
  }

  .post-link-title {
    display: block;
    font-size: 0.875rem;
    font-weight: 500;
    color: #374151;
    margin-bottom: 0.25rem;
    line-height: 1.4;
  }

  .post-link:hover .post-link-title {
    color: #3b82f6;
  }

  .post-link.active .post-link-title {
    color: #3b82f6;
    font-weight: 600;
  }

  .post-link-date {
    display: block;
    font-size: 0.75rem;
    color: #9ca3af;
    font-weight: 400;
  }

  /* 滚动条样式 - 始终显示 */
  .sidebar::-webkit-scrollbar {
    width: 8px;
  }

  .sidebar::-webkit-scrollbar-track {
    background: #f9fafb;
    border-left: 1px solid #f3f4f6;
  }

  .sidebar::-webkit-scrollbar-thumb {
    background: #d1d5db;
    border-radius: 4px;
  }

  .sidebar::-webkit-scrollbar-thumb:hover {
    background: #9ca3af;
  }

  /* 确保滚动条始终显示 */
  .sidebar {
    scrollbar-width: thin;
    scrollbar-color: #d1d5db #f9fafb;
  }

  @media (max-width: 1200px) {
    .sidebar {
      display: none;
    }
  }
</style>
