# x1ngg3 的个人博客

这是我使用 [Astro](https://astro.build) 构建的个人博客，部署在 GitHub Pages 上。

## ✨ 特性

- 📝 **文章系统** - 支持 Markdown 格式的技术文章
- 💬 **说说功能** - 记录生活碎碎念和吐槽
- 🏷️ **标签分类** - 方便按主题浏览内容
- 🎨 **简洁设计** - 专注于内容阅读体验
- 📱 **响应式布局** - 完美适配移动端
- ⚡ **极速加载** - 纯静态页面，秒开
- 🔍 **SEO 优化** - 搜索引擎友好

## 📁 项目结构

```
blog/
├── .github/workflows/     # GitHub Actions 自动部署
├── public/               # 静态资源（图片等）
├── src/
│   ├── components/       # 组件
│   ├── content/          # 👈 你的内容目录
│   │   ├── posts/       # 👈 写文章的地方
│   │   └── thoughts/    # 👈 写说说的地方
│   ├── layouts/          # 布局
│   ├── pages/            # 页面路由
│   └── styles/           # 样式
└── astro.config.mjs      # Astro 配置
```

## 🚀 快速开始

### 首次使用（克隆项目）

```bash
# 1. 克隆仓库
git clone https://github.com/x1ngg3/blog.git
cd blog

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 浏览器访问 http://localhost:4321
```

### 换电脑时（3个命令搞定）

```bash
git clone https://github.com/x1ngg3/blog.git
cd blog && npm install
npm run dev
```

## ✍️ 写作流程

### 写新文章

1. 在 `src/content/posts/` 创建新的 `.md` 文件
2. 添加 Front Matter 和内容：

```markdown
---
title: "文章标题"
description: "文章简介"
date: 2024-01-24
tags: ["标签1", "标签2"]
---

文章内容...
```

3. 提交并推送：

```bash
git add .
git commit -m "新文章：xxx"
git push
```

4. 等待 1 分钟，GitHub Actions 自动部署完成！

### 写说说

1. 在 `src/content/thoughts/` 创建新的 `.md` 文件
2. 添加内容：

```markdown
---
date: 2024-01-24T10:00:00Z
mood: "😊"
---

今天天气不错！
```

3. 同样推送即可自动部署

## 🛠️ 常用命令

| 命令 | 说明 |
|-----|------|
| `npm run dev` | 启动开发服务器（本地预览） |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览构建结果 |

## 🎨 自定义配置

### 修改网站信息

编辑 `astro.config.mjs`：

```javascript
export default defineConfig({
  site: 'https://x1ngg3.github.io',  // 👈 改成你的域名
  // 如果部署在子目录，取消下面注释
  // base: '/blog',
});
```

### 修改样式

- **全局样式**: 编辑 `src/styles/global.css`
- **主题色**: 修改 CSS 变量 `--color-primary`

### 修改个人信息

- **导航栏**: 编辑 `src/layouts/BaseLayout.astro`
- **关于页面**: 编辑 `src/pages/about/index.astro`
- **首页欢迎语**: 编辑 `src/pages/index.astro`

## 🌐 部署说明

### GitHub Pages 部署

本项目已配置好 GitHub Actions 自动部署，你只需要：

1. **首次部署**：
   - 在 GitHub 仓库设置中，进入 `Settings` → `Pages`
   - Source 选择 `GitHub Actions`
   - 推送代码到 `main` 分支即可自动部署

2. **后续部署**：
   - 每次推送到 `main` 分支都会自动触发部署
   - 无需任何手动操作

### 自定义域名（可选）

如果你有自己的域名：

1. 在 `public/` 目录创建 `CNAME` 文件
2. 文件内容写入你的域名，如：`blog.example.com`
3. 在域名提供商配置 CNAME 记录指向 `<username>.github.io`

## 📝 写作技巧

### Front Matter 字段说明

**文章 (posts):**

```yaml
---
title: "标题"               # 必填
description: "简介"         # 必填
date: 2024-01-24           # 必填
tags: ["标签1", "标签2"]    # 可选
category: "分类"           # 可选
author: "作者名"           # 可选，默认 x1ngg3
draft: false               # 可选，true 为草稿
---
```

**说说 (thoughts):**

```yaml
---
date: 2024-01-24T10:00:00Z  # 必填
mood: "😊"                  # 可选，心情 emoji
tags: ["标签"]              # 可选
---
```

### Markdown 语法支持

- ✅ 标题、段落、列表
- ✅ 粗体、斜体、代码
- ✅ 链接、图片
- ✅ 代码块（带语法高亮）
- ✅ 引用、表格
- ✅ 任务列表

## 🤖 AI 协作提示

如果你让 AI 帮忙修改博客，只需要说：

- "把主题色改成蓝色"
- "文章卡片加个阴影"
- "首页显示 10 篇文章"
- "添加深色模式"

AI 会直接修改代码，你审查后确认即可。

## 📦 技术栈

- **框架**: [Astro](https://astro.build)
- **样式**: 纯 CSS (CSS Variables)
- **部署**: GitHub Pages
- **CI/CD**: GitHub Actions
- **代码高亮**: Shiki (内置)

## 🐛 常见问题

### Q: 本地运行报错？

**A:** 确保 Node.js 版本 >= 18

```bash
node -v  # 查看版本
```

### Q: 推送后没有自动部署？

**A:** 检查：
1. GitHub Actions 是否启用
2. 仓库 Settings → Pages → Source 是否设置为 `GitHub Actions`
3. 查看 Actions 标签页的构建日志

### Q: 如何添加评论功能？

**A:** 后期可以集成 [Giscus](https://giscus.app)（基于 GitHub Discussions），完全免费。

### Q: 可以添加搜索功能吗？

**A:** 可以！让 AI 帮你集成 [Pagefind](https://pagefind.app/) 或 [Fuse.js](https://fusejs.io/)

## 📄 License

MIT License - 随意使用和修改

## 🙏 致谢

- [Astro](https://astro.build) - 优秀的静态站点生成器
- [GitHub Pages](https://pages.github.com) - 免费的托管服务
- [Shiki](https://shiki.matsu.io) - 代码高亮

---

**Enjoy blogging! 📝✨**

如有问题，欢迎提 [Issue](https://github.com/x1ngg3/blog/issues)
